# Hands-on Assignment 5 – Self Assessment

## Checklist

### Data Linking

* [✓] Created a JSON file with the operations performed over the data for linking (`*-with-links.json`) under the `lodrefine/` directory  
* [✓] Created a CSV file with the updated versions of the datasets (`*-with-links.csv`) under the `csv/` directory  

### Updated Mappings

* [✓] Created an RML mapping file with the updated versions of the mappings (`*-with-links.rml`) under the `mappings/` directory  

### RDF Generation

* [✓] Produced an RDF file in Turtle syntax with the linked data (`*-with-links.ttl`) under the `rdf/` directory  

### SPARQL Queries

* [✓] Created a SPARQL file with queries to verify the generated links (`queries-with-links.sparql`) under the `rdf/` directory  
* [✓] Confirmed that the queries return the expected results based on the linked dataset  

### Documentation

* [✓] Added a `selfAssessmentHandsOn5.md` file in the group root directory

## Comments on the Self-Assessment
